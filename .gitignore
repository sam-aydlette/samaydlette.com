# Terraform files
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform/
.terraform.lock.hcl
tfplan
tfplan.json
infrastructure/*.tfstate
infrastructure/*.tfstate.*
infrastructure/tfplan
compliance-result.json

# Lambda deployment packages
*.zip
lambda/node_modules/
lambda/policies.rego
# Compiled OPA Wasm bundle. Built from policies.rego at deploy time; not
# committed because it is a deterministic artifact of the source.
infrastructure/lambda/policy.wasm
*.wasm

# Local-only validation script. Personal tooling; not part of the deployed
# pipeline.
scripts/validate-locally.sh

# Locally-generated dashboard artifacts. The CI workflow publishes these
# directly to S3 at /.well-known/, not via the website/ tree, so they are
# never committed. Generated for local viewer.html testing only.
website/.well-known/ksi-signal.json
website/.well-known/ksi-signal-runtime.json
website/.well-known/ksi-signal.schema.json
website/.well-known/ksi-signal.bundle
website/.well-known/oscal-ssp.json
website/.well-known/vdr-report.json
infrastructure/vdr-report.json
website/.well-known/iiw.csv
infrastructure/iiw.csv
website/.well-known/oscal-poam.json
infrastructure/oscal-poam.json
infrastructure/oscal-ssp.json
infrastructure/ksi-signal.json
infrastructure/cmmc-out.json

# OPA temporary files
opa-input.json
resource-input.json
opa-result.json
compliance-result.json

# AWS CLI output
aws-cli-output.json

# Logs
*.log

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# Backup files
*.bak
*.backup

# Environment files
.env
.env.local
.env.*.local

# Coverage reports
coverage/
*.lcov

# Dependency directories
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# SSL certificates (if stored locally)
*.pem
*.key
*.crt

# Compliance reports (if generated locally)

# Infrastructure directory specific
infrastructure/node_modules/
infrastructure/package*.json
infrastructure/*.zip
infrastructure/*-test.json
infrastructure/test-results/
infrastructure/ponse*
infrastructure/lambda\ invoke*

# Generated SCuBA bundle (regenerable via scripts/build-scuba-bundle.py; signed+published in the pipeline)
scuba/dist/

# Python test cache
__pycache__/
*.pyc
tests/__pycache__/

# Stale committed inventory snapshot — authoritative copies are generated+signed in CI and published at /.well-known/
/ksi-signal.json
/ksi-signal.bundle

# Working/planning docs — kept local, not pushed to main (operator preference)
/TASKS.md
/docs/assessment/
