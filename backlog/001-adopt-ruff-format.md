# Run `ruff format` across `scripts/` and `tests/` and make CI enforce it from then on.
REPO: samaydlette.com
STATUS: QUEUED            # QUEUED | IN_PROGRESS | PARKED | DONE | FAILED
ACCEPTANCE: `make check` passes, plus: `python3 -m ruff format --check scripts/ tests/` exits 0 on a clean tree; the deploy workflow's lint step runs `ruff format --check scripts/ tests/` as a blocking step and fails when a file is unformatted (prove it by unformatting one file locally and seeing the command exit non-zero).
OUT OF SCOPE: Changing ruff's lint rule selection in `pyproject.toml` (`select = ["F", "B"]`, `ignore = ["B007"]`) — this task changes formatting only, never which lints are enforced. Widening the format scope past `scripts/` and `tests/` (CI's lint scope is exactly those two; widening here without widening CI breaks the local-green-means-CI-green property the root Makefile was built for). Reformatting `website/`, `tools/`, `infrastructure/`, or any JS. Any behaviour change: this diff must be formatting-only, and any line that changes meaning is a bug in the task, not a bonus. Bumping the `ruff==0.15.20` pin. Touching `docs/poam.md`, `website/.well-known/`, the reconciliation gate, or the OPA gate.
LANDMINES: This rewrites 76 of 77 Python files in one commit (measured 2026-08-25 on `main` @ 4223377: `ruff format --check scripts/ tests/` → "76 files would be reformatted, 1 file already formatted"; it was 70 of 71 on 2026-08-24, and the count grows with the tree — re-measure before quoting it in a PR). It will conflict with every open branch that touches Python, so land it when the tree is quiet and rebase the others onto it rather than the reverse. `make fmt` already exists in the root Makefile and is deliberately left out of `make check` with a warning comment explaining exactly this — read that comment before changing the target. The root `Makefile` and its `make check` / `check-full` targets are on `main` (#305 merged as `e124041`), so the ACCEPTANCE commands above run as written. `ruff format` reflows `assert` and long call chains, so review the test diffs for anything that changed a string literal.
---
ASSUMPTION: this task includes wiring `ruff format --check` into `.github/workflows/deploy-with-opa.yml` as a blocking step, not just the one-time reformat. Reason: a reformat with no enforcement drifts back within weeks and the 70-file churn gets paid twice. If you only wanted the reformat, delete the CI half of ACCEPTANCE and say so — the reformat commit stands on its own.

Diagnosed 2026-08-24 during the Session 2 verification-interface work; deliberately deferred there because it is its own commit.

The situation today: `ruff` is pinned at `0.15.20` and CI runs `ruff check scripts/ tests/` (lint only). `ruff format` has never been run against this codebase, so 70 of 71 files are unformatted by its rules. The root Makefile carries a `fmt` target that would do it, with a warning comment saying CI does not enforce it and that adopting it is a separate commit — this is that commit.

Do it in this order, and keep the two halves as two commits in one PR so the reformat is reviewable by ignoring it:

1. Run `make fmt` (or `python3 -m ruff format scripts/ tests/`), commit the pure reformat with a message that says it is mechanical and names the exact ruff version that produced it. Add the commit SHA to `.git-blame-ignore-revs` if the repo has one, or create it — otherwise every `git blame` in this repo points at this commit forever.
2. Add `ruff format --check scripts/ tests/` next to the existing `ruff check` step in the deploy workflow, and to the root Makefile's `lint` target so local and CI agree. Update the `fmt` target's warning comment, which will then be stale.

Worth knowing: `InsightWeaver` already has zero format drift (111 files, all formatted) but its `make check` does not run `--check` while its CI does. That is the mirror-image bug and it has its own backlog entry in that repo. This repo has the opposite problem — nothing enforces it anywhere.
